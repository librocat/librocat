{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "librocat": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/server/main.js"],
      "env": {
        "LIBROCAT_BUNDLE": "${PLUGIN_DATA}/okf"
      }
    }
  }
}
